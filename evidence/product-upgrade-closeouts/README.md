# Fresh public closeout observations

These JSON files were generated by the new read-only collector on 10 September 2026 against the existing team-controlled deployments. Each contains the collected observations and consistency result. `main120-fresh-reimport.json` was recollected using the first export as a locator through `--input`; supplied verdicts and observation fields were discarded.

All four distinct epochs report complete, with zero outstanding earned payment. Their old orders remain labeled legacy: these runs establish no new portable consent, independent buyer or new settlement. RETURN is recognized owner-level free credit; an individual free withdrawal is not attributed to an epoch. See `docs/PRODUCT-UPGRADE-RELEASE.md` for amounts, scope and reproduction.
