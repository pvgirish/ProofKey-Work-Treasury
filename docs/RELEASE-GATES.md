# Release evidence ledger

This ledger separates implemented behavior, local verification and public observations. A pending row is not a passed release gate. The built-in public run uses team-controlled actors.

| Gate | Implemented / local evidence | Public observation |
|---|---|---|
| Frozen allocation/checkpoint ABI, epoch/order domains | `schema/`, SDK conformance and `IdentityConformance.t.sol` | Deployed runtime correspondence and public domain readback pass |
| Conserved source policy and all terminal outcomes | `SourceCoordinator.t.sol`, `AdversarialIntegration.t.sol` | Main journey and the smaller public branch trace complete; pending expiry, no delivery, approval, monitoring default, quorum, committee timeout, mutual settlement, stale/duplicate refusals and both mutual/default orderings are recorded |
| Capacity, actual 113-leaf policy, actual 128-leaf tree | `SourceCoordinator.t.sol` reaches the 32-reservation cap with money remaining, rejects reservation 33 without mutation, then completes protected returns and all obligations through the actual 113-leaf production-policy trace; `AllocationTree.t.sol` runs the actual production tree through 128 and rejects append 129 | Smaller public traces and bounded getters are observed. The required maximum traces are executable local production-code tests; no separate maximum-size public run is required |
| Atomic publication and independent rebuild | The 113-leaf policy trace checks unchanged tree state after every nonallocation transition, independently rebuilds after every allocation-producing call, and checks every final checkpoint payload; the separate 128-leaf harness rebuilds after every append | Main and branch full-leaf rebuilds match their native-authenticated checkpoints; the public WORK/FEE multi-leaf receipt exposes its final checkpoint in the same transaction |
| Republish and immutable source authority | Source tests verify permissionless exact same-state and phase-only checkpoint payloads with full state/config immutability; Safe integration tests pass | Two public post-close republishes exactly match the final sweep payload and leave financial, leaf and configuration state unchanged. Disposable Safe source rotation is complete; its target attestation remains pending |
| Native necessity, single/batch/segmented paths | `WorkTreasuryTarget.t.sol`; local native boundary stub disclosed | Actual single, batch and segmented native transactions have all mined successfully |
| Persisted cache, wrong event/emitter/domain rejection | Target and adversarial tests | Cached main claims completed. An authenticated checkpoint presented as an allocation produced the recorded status-0 mined semantic refusal |
| Receipt/checkpoint economic equivalence and replay | Both directions in `EndToEnd.t.sol` | Both root-to-receipt and receipt-to-root replay attempts refuse the already recognized economic right |
| Refund before unseen WORK; payout ordering and recovery | Target tests and locked 120-unit E2E cover rejecting recipients, accepting-lock finality, and both successful fixed/owner orderings for claims and free balances. Successor funding uses only the refund owner's returned free balance under a fresh epoch ID while the old reserve remains isolated | RETURN50 was recognized while B remained unresolved; the journey later paid 55 CTC to workers, withdrew 65 CTC for the refund beneficiary and ended with reserve zero |
| Receipt-only and cached-root collection | Target and E2E tests | Public branch WORK/FEE collection succeeds from a noninitial exact receipt without siblings; main worker collection succeeds from the cached final root after proof-provider access stops |
| Measured costs and transaction bounds | Local matrix in `evidence/local-performance.json`; isolated canonical AllocationCreated event: 4,496 gas; CheckpointPublished event: 2,491 gas | Native batch: 435,498 gas; single final-checkpoint import: 395,958 gas; segmented branch authentication: 428,008 gas; receipt-only WORK/FEE recognition: 572,584/538,422 gas |
| Wallet app, portable evidence, proof replacement | Guided app, 15 SDK tests and browser review pass | [Public app](https://pvgirish.github.io/ProofKey-Work-Treasury/) returns HTTP 200; live-readback and browser-proof reports pass. Regenerated hosted proof preserves exact transaction bytes and ten continuity roots, and the fixed native verifier returns true in a read-only call |
| Completed-WORK consumer | Target tests reject nonpayments and wrong expectations | Public PaidInvoiceBook records the main journey's completed WORK withdrawal |
| Public CI | `.github/workflows/ci.yml` | [Run 34392863030](https://github.com/pvgirish/ProofKey-Work-Treasury/actions/runs/34392863030) passes 40 contract tests and 15 SDK tests. A follow-up run for a script-only refund-withdraw reporting fix is pending; production contracts are unchanged |
| Two consenting independent settlements, repeat use, buyer reason | Participant request sent to project owner | **Pending real participants; cannot be replaced by team fixtures** |

The runtime sizes before deployment are SourceCoordinator 21,789 bytes and WorkTreasury 20,019 bytes, below the 24,576-byte EIP-170 limit. Compiler settings are Solidity 0.8.28, Cancun, optimizer 200, via IR. The deployment runner compares deployed bytes to the linked compiled runtime outside declared immutable slots and checks the public target immutables. WorkTypes, AllocationTree and WorkTreasury are fully source-verified on Creditcoin testnet Blockscout. `evidence/native-recovery-verification-33642ef4.json` records the read-only hosted-provider replacement and fixed-native-verifier result, and `docs/PUBLIC-EVIDENCE.md` links the current public journals and receipts.

Remaining automated target work is limited to authenticating and collecting the already-mined successor-expiry RETURN and the disposable-Safe fixture's allocations/RETURN. The follow-up CI run for the demo reporting fix also remains pending. The only external release gate is two consenting independent settlements, repeat use and a real buyer reason; team-controlled public fixtures cannot satisfy it.
