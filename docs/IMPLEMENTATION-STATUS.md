# Work Treasury implementation status

Implementation began on 9 September 2026 from the final architecture lock. This is a separate repository; frozen V1 code and Review #41 remain unchanged.

Model allocation: GPT-5.6 Sol at high reasoning handles the bounded source, target and application implementation tasks. GPT-6 Astra coordinates the shared commitments and reviews financial/security boundaries. This records the actual coding workflow, not a claim that a ChatGPT UI setting was changed.

| Area | Current status |
|---|---|
| Shared epoch/order/allocation codecs and incremental tree | Implemented; cross-language vectors, the reachable 113-leaf production-policy trace and the 128-leaf production-tree harness pass locally |
| Source work policy and finite budget lifecycle | Implemented; every terminal and required refusal passes locally, and the smaller public main and branch traces are complete on Sepolia |
| Target native authentication, one recognition kernel and withdrawals | Implemented; local tests disclose their native VM stub. Actual single, batch and segmented native paths have each mined successfully on Creditcoin testnet |
| Wallet app, portable evidence and inspector | Implemented; 15 SDK tests and desktop/mobile browser checks pass. Fresh sessions now verify the public setup and read both chains automatically; saved settings remain unchanged until an explicit action. The [public app](https://pvgirish.github.io/ProofKey-Work-Treasury/) returns HTTP 200. Finalized completed-payment readback, claim import, browser proof, and provider-independent raw proof regeneration are recorded |
| Safe authority/owner change integration | Actual Safe v1.4.1 local integration passes. A new disposable public Safe rejected the removed-owner signature set with GS026 and accepted the same queued approval under its current owners; the existing Safe remained unchanged. Its pre-rotation allocation and later allocation were both collected on Creditcoin, and its refund was withdrawn |
| Actual native testnet evidence and measured costs | Deployed runtime correspondence passes, and all three target-side contracts/libraries are fully verified on Blockscout. The complete 120-CTC journey, branch receipt-only WORK/FEE collection and measured native calls are recorded; successor-expiry and rotation collection/refunds are complete |
| Public CI and submission artifacts | The baseline [public CI run 34394846059](https://github.com/pvgirish/ProofKey-Work-Treasury/actions/runs/34394846059) passed 40 contract tests and 15 SDK tests. The reviewed upgrade passes 46 Foundry results and 15 SDK tests locally; its public CI is recorded separately in the release manifest. Production contracts and deployed addresses are unchanged |
| Completed-WORK consumer | Implemented and tested locally; the main public journey deployed PaidInvoiceBook and recorded an actually withdrawn WORK payment |
| Two consenting independent settlements and buyer reference | External participants needed; request sent to user |

The public main journey is complete: workers received 55 CTC, the refund beneficiary withdrew 65 CTC, and the epoch reserve is zero. RETURN50 was recognized while order B still held 40 CTC unresolved. The final checkpoint then supported delayed cached-root worker collection. Both cross-route replay directions refused duplicate value, and a deliberately mined status-0 transaction demonstrated that an authenticated checkpoint event cannot be used as an allocation payment fact.

The source branch fixture is complete with E=9 CTC and R=11 CTC; its noninitial WORK/FEE receipt-only allocations were recognized and 9 CTC was paid. Its returned balance funded a fresh successor epoch; source-proven uninitialized expiry was authenticated on Creditcoin and the combined 11 CTC refund was withdrawn. The disposable Safe rotation also completed: 2 CTC paid and 1 CTC refunded, with its old allocation collectible after the owner change. These team-controlled fixtures remain distinct from the still-pending two independent settlements and buyer reference. Current links and transaction receipts are collected in `docs/PUBLIC-EVIDENCE.md`; local measurements and proof-recovery evidence remain in `evidence/local-performance.json` and `evidence/native-recovery-verification-33642ef4.json`.

The [final cross-chain readback](../evidence/release-readback.json) pins Sepolia block 11,669,888 and Creditcoin block 5,459,393. All four source epochs are closed, each independently reconstructed allocation root matches storage, every economic right is recognized, all worker claims are withdrawn and every target reserve is zero. Global accounting confirms 143 testnet CTC deposited, 143 withdrawn, zero live liabilities and zero treasury balance. The [published-app check](../evidence/ui-completed-hosted-qa/report.md) also verifies completed payments, the recorded invoice and portable claim import. Automated implementation and public demonstration gates are complete. Independent settlements, repeat independent operation and a buyer reference remain external release requirements.

## Review-driven upgrade, 10 September 2026

[Conservation and capacity reuse](CONSERVATION-AND-REUSE.md) states the source partition, target backing obligation and global liability accounting, with explicit assumptions. Four invariant assertions share one Foundry campaign of 128 runs × 64 randomized handler calls (8,192 calls, including possible guarded no-ops); two additional fuzz properties exercise reuse and irreversible RETURN. The complete suite reports 46 Foundry results, not 49: the four invariant assertions are grouped into one campaign result.

The [refusal ledger](REFUSAL-LEDGER.md) separates local false/reverting-verifier and rollback tests, read-only public refusals and the existing mined semantic refusal. At target block 5,459,734, the old ten-root proof was refused for a continuity mismatch and a regenerated ninety-root proof for the exact same source transaction was accepted. This is a captured read-only native-verifier observation, not a new mined settlement or a promise of future proof availability.

The [first-load UI checks](../evidence/ui-firstload-qa/report.md) pass 19 checks, including automatic public reads, independent tree rebuild, saved-setting preservation and explicit recovery. These improvements add verification and evidence without a new financial path. Independent use and a real buyer remain pending.
